// solve this
#include<bits/stdc++.h>
using namespace std;
#define debug(x) cout<<#x<<" = "<<x<<endl;
#define sz(x) (int) (x).size()

int main() {
    int t = 1;
    cin >> t;
    while (t--) {
        int n;
        scanf("%d", &n);
        vector <int> a(n);
        set <int> check(n);
        vector <int> cnt(n);
        for (int i = 0; i < n; i++) {
            scanf("%d", &a[i]):
            cnt[i] = cnt[i-1]
            if (check.count(a[i])) {

    }
}