#include "bits/stdc++.h"

#ifdef local
#include "custom/debugger.h"
#else
#define debug(...) 42;
#endif

using namespace std;
#define int long long

// really cool graph problem
void solve([[maybe_unused]] int test) {
    string s;
    cin >> s;
    int m = (int)s.size();

    for (int len = 1; len <= m; len++) {
    }
}

int32_t main() {
    int t = 1;
    cin >> t;

    for (int tt = 1; tt <= t; tt++) {
        solve(tt);
    }
}
